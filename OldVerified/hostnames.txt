$hostnames = @("projetos.imd.ufrn.br")

foreach ($hostname in $hostnames) {
    try {
        $resolve = [System.Net.Dns]::GetHostAddresses($hostname)
        Write-Host "$hostname resolve para: $($resolve.IPAddressToString)"
    } catch {
        Write-Host "Falha ao resolver $hostname"
    }
}